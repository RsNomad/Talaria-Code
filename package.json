{
  "name": "talaria-code",
  "displayName": "Talaria Code",
  "description": "Talaria Code — an in-editor client for the Hermes agent: chat, tools, MCP, skills, checkpoints and more, in a native VS Code panel.",
  "version": "0.1.4",
  "publisher": "syntinal",
  "license": "GPL-3.0-or-later",
  "repository": {
    "type": "git",
    "url": "https://github.com/RsNomad/Talaria-Code.git"
  },
  "engines": {
    "vscode": "^1.125.0",
    "node": ">=18"
  },
  "categories": [
    "AI",
    "Chat",
    "Other"
  ],
  "main": "./dist/extension.js",
  "workspaces": [
    "webview"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "extensionKind": [
    "workspace"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": "limited",
      "description": "In an untrusted (Restricted Mode) workspace Talaria Code runs only its process-free mock backend: it will not spawn the Hermes agent (`hermes acp` / `python -m tui_gateway.entry`), will not index the codebase (file walk + embeddings upload), and will not bind the read-only LSP-over-HTTP MCP server that exposes live language-server intelligence (diagnostics, definitions, references, symbols, hover) to the agent. Grant Workspace Trust to enable the real backend, RAG indexing, and LSP tools.",
      "restrictedConfigurations": [
        "talaria.hermesPath",
        "talaria.pythonPath",
        "talaria.cwd",
        "talaria.backend",
        "talaria.dashboardPort",
        "talaria.dashboardAdopt",
        "talaria.autocomplete.backend",
        "talaria.autocomplete.endpoint",
        "talaria.autocomplete.apiKey",
        "talaria.nextEdit.source",
        "talaria.nextEdit.endpoint",
        "talaria.rag.embedEndpoint",
        "talaria.rag.indexDir",
        "talaria.lib.enabled",
        "talaria.customModes"
      ]
    },
    "virtualWorkspaces": {
      "supported": "limited",
      "description": "In a virtual workspace Talaria Code cannot spawn its local child processes or index a real on-disk codebase; only the mock backend and inline autocomplete are available."
    }
  },
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "talaria",
          "title": "Talaria Code",
          "icon": "media/talaria.svg"
        }
      ]
    },
    "views": {
      "talaria": [
        {
          "type": "webview",
          "id": "talaria.panel",
          "name": "Talaria Code"
        }
      ]
    },
    "commands": [
      {
        "command": "talaria.newSession",
        "title": "Restart Agent Connection",
        "category": "Talaria",
        "icon": "$(debug-restart)"
      },
      {
        "command": "talaria.openSettings",
        "title": "Open Settings",
        "category": "Talaria",
        "icon": "$(gear)"
      },
      {
        "command": "talaria.openSetup",
        "title": "Backend Setup",
        "category": "Talaria",
        "icon": "$(rocket)"
      },
      {
        "command": "talaria.showLogs",
        "title": "Show Logs",
        "category": "Talaria",
        "icon": "$(output)"
      },
      {
        "command": "talaria.setAutocompleteApiKey",
        "title": "Set Autocomplete API Key",
        "category": "Talaria"
      },
      {
        "command": "talaria.addToChat",
        "title": "Add to Talaria",
        "category": "Talaria"
      },
      {
        "command": "talaria.explainCode",
        "title": "Explain with Talaria",
        "category": "Talaria"
      },
      {
        "command": "talaria.improveCode",
        "title": "Improve with Talaria",
        "category": "Talaria"
      },
      {
        "command": "talaria.fixCode",
        "title": "Fix with Talaria",
        "category": "Talaria"
      },
      {
        "command": "talaria.acceptDiff",
        "title": "Accept (Talaria)",
        "category": "Talaria",
        "icon": "$(check)"
      },
      {
        "command": "talaria.rejectDiff",
        "title": "Reject (Talaria)",
        "category": "Talaria",
        "icon": "$(close)"
      },
      {
        "command": "talaria.generateCommitMessage",
        "title": "Generate Commit Message",
        "category": "Talaria",
        "icon": "$(sparkle)"
      },
      {
        "command": "talaria.nextEdit.jump",
        "title": "Next Edit Suggestions: Jump to Proposal",
        "category": "Talaria"
      },
      {
        "command": "talaria.nextEdit.accept",
        "title": "Next Edit Suggestions: Accept Proposal",
        "category": "Talaria"
      },
      {
        "command": "talaria.nextEdit.dismiss",
        "title": "Next Edit Suggestions: Dismiss Proposal",
        "category": "Talaria"
      },
      {
        "command": "talaria.nextEdit.onFimAccept",
        "title": "Next Edit Suggestions: On Autocomplete Accepted",
        "category": "Talaria"
      }
    ],
    "keybindings": [
      {
        "command": "talaria.nextEdit.jump",
        "key": "tab",
        "when": "talaria.nextEdit.jumpVisible && !talaria.nextEdit.jumped && editorTextFocus && !editorReadonly && !suggestWidgetVisible && !inlineSuggestionVisible && !inlineEditIsVisible && !inSnippetMode && !editorTabMovesFocus"
      },
      {
        "command": "talaria.nextEdit.accept",
        "key": "tab",
        "when": "talaria.nextEdit.jumpVisible && talaria.nextEdit.jumped && editorTextFocus && !editorReadonly && !suggestWidgetVisible && !inlineSuggestionVisible && !inlineEditIsVisible && !inSnippetMode && !editorTabMovesFocus"
      },
      {
        "command": "talaria.nextEdit.dismiss",
        "key": "escape",
        "when": "talaria.nextEdit.jumpVisible && editorTextFocus && !suggestWidgetVisible && !inlineSuggestionVisible && !inlineEditIsVisible && !inSnippetMode"
      }
    ],
    "submenus": [
      {
        "id": "talaria.editorSubmenu",
        "label": "Talaria",
        "icon": "media/talaria.svg"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "talaria.nextEdit.jump",
          "when": "false"
        },
        {
          "command": "talaria.nextEdit.accept",
          "when": "false"
        },
        {
          "command": "talaria.nextEdit.dismiss",
          "when": "false"
        },
        {
          "command": "talaria.nextEdit.onFimAccept",
          "when": "false"
        }
      ],
      "view/title": [
        {
          "command": "talaria.openSetup",
          "when": "view == talaria.panel",
          "group": "navigation@1"
        },
        {
          "command": "talaria.openSettings",
          "when": "view == talaria.panel",
          "group": "navigation@2"
        }
      ],
      "editor/context": [
        {
          "submenu": "talaria.editorSubmenu",
          "when": "editorHasSelection && talaria.ready",
          "group": "1_modification"
        }
      ],
      "editor/title": [
        {
          "command": "talaria.acceptDiff",
          "when": "resourceScheme == talaria-diff",
          "group": "navigation@1"
        },
        {
          "command": "talaria.rejectDiff",
          "when": "resourceScheme == talaria-diff",
          "group": "navigation@2"
        }
      ],
      "scm/title": [
        {
          "command": "talaria.generateCommitMessage",
          "when": "scmProvider == git && talaria.ready",
          "group": "navigation"
        }
      ],
      "talaria.editorSubmenu": [
        {
          "command": "talaria.addToChat",
          "when": "editorHasSelection && talaria.ready",
          "group": "1_talaria@1"
        },
        {
          "command": "talaria.explainCode",
          "when": "editorHasSelection && talaria.ready",
          "group": "1_talaria@2"
        },
        {
          "command": "talaria.improveCode",
          "when": "editorHasSelection && talaria.ready",
          "group": "1_talaria@3"
        }
      ]
    },
    "walkthroughs": [
      {
        "id": "talariaBackendSetup",
        "title": "Set up Talaria Code",
        "description": "Install the Hermes agent and connect your local models from one panel.",
        "steps": [
          {
            "id": "openBackendSetup",
            "title": "Open Backend Setup",
            "description": "Open the Talaria panel, then run Backend Setup to install the agent (via pipx), configure your provider, and pick your autocomplete/RAG models — no hand-edited JSON.\n[Open Backend Setup](command:talaria.openSetup)",
            "media": {
              "markdown": "media/walkthrough-setup.md"
            },
            "completionEvents": [
              "onCommand:talaria.openSetup"
            ]
          }
        ]
      }
    ],
    "configuration": [
      {
        "title": "Backend & Agent",
        "order": 1,
        "properties": {
          "talaria.backend": {
            "order": 1,
            "type": "string",
            "enum": [
              "mock",
              "acp"
            ],
            "enumDescriptions": [
              "Canned streaming + panel data. Spawns no process; runs on any OS. Default.",
              "Real backend: spawns `hermes acp` over JSON-RPC/stdio (Linux). Wired later."
            ],
            "default": "mock",
            "scope": "machine",
            "description": "Which agent backend the extension talks to. Machine-scoped: a workspace cannot flip this to `acp` to force a process spawn (security)."
          },
          "talaria.hermesPath": {
            "order": 2,
            "type": "string",
            "default": "",
            "scope": "machine",
            "markdownDescription": "Absolute path to the `hermes` executable (e.g. a virtualenv `bin/hermes`). Leave empty to discover it from the login-shell `PATH` (`command -v hermes`). Set this when PATH discovery cannot work — Flatpak VS Code, Remote-SSH with a non-login PATH, or a custom install location. Ignored while `#talaria.backend#` is `mock`. Machine-scoped: a workspace cannot override it (prevents a malicious repo from repointing the executable — security)."
          },
          "talaria.pythonPath": {
            "order": 3,
            "type": "string",
            "default": "",
            "scope": "machine",
            "markdownDescription": "Absolute path to the Python interpreter used to launch the real Hermes backend (e.g. a virtualenv `bin/python`). Leave empty to derive it from the discovered `hermes` executable's own environment (its venv `bin/python`, following symlinks — works for pipx installs). Set this when that derivation cannot work (Flatpak VS Code, Remote-SSH, or a custom install layout). Ignored while `#talaria.backend#` is `mock`. Machine-scoped: a workspace cannot override this (prevents a malicious repo from repointing the interpreter — security)."
          },
          "talaria.cwd": {
            "order": 4,
            "type": "string",
            "default": "",
            "scope": "machine",
            "markdownDescription": "Working directory for the Hermes agent process. Leave empty to use the first workspace folder. Ignored while `#talaria.backend#` is `mock`. Machine-scoped: not workspace-overridable (security). When no workspace folder is open, the user home directory is used."
          },
          "talaria.agent.localModel.modelId": {
            "order": 5,
            "type": "string",
            "default": "",
            "scope": "machine",
            "markdownDescription": "Catalog id of the local model saved for the agent via the Setup panel's \"Configure Local Agent Model\" block (e.g. `devstral-24b`). Leave empty when no local agent model has been saved. Set through the panel, not hand-edited. Machine-scoped: a workspace cannot silently repoint the agent at a different local model (security — same rationale as `#talaria.autocomplete.model#`)."
          },
          "talaria.agent.localModel.backend": {
            "order": 6,
            "type": "string",
            "enum": [
              "ollama",
              "llamacpp",
              "vllm"
            ],
            "default": "ollama",
            "scope": "machine",
            "markdownDescription": "Which backend serves the saved local agent model (`#talaria.agent.localModel.modelId#`). Only meaningful once a model has been saved. Machine-scoped: a workspace cannot flip this to redirect where agent-model traffic goes (security)."
          },
          "talaria.agent.localModel.endpoint": {
            "order": 7,
            "type": "string",
            "default": "",
            "scope": "machine",
            "markdownDescription": "Base URL for the saved local agent model's backend. Leave empty when no local agent model has been saved. Set through the Setup panel's Save action, not hand-edited. Machine-scoped: a workspace cannot redirect where agent-model requests are sent (security)."
          }
        }
      },
      {
        "title": "Autocomplete (FIM)",
        "order": 2,
        "properties": {
          "talaria.autocomplete.enabled": {
            "order": 1,
            "type": "boolean",
            "default": true,
            "description": "Enable Talaria inline (FIM) autocomplete."
          },
          "talaria.autocomplete.backend": {
            "order": 2,
            "type": "string",
            "enum": [
              "ollama",
              "llamacpp",
              "vllm",
              "codestral",
              "openai-compat"
            ],
            "default": "ollama",
            "scope": "machine",
            "markdownDescription": "Which FIM backend serves autocomplete requests. Machine-scoped: a workspace cannot flip this to a cloud backend and redirect your code off the machine (security — this setting SELECTS the endpoint). Note on `openai-compat`: a strictly-spec OpenAI server can reject the request with HTTP 400 — Talaria sends more than four `stop` sequences and a `skip_special_tokens` field, which some servers refuse. If yours does, use the `vllm`, `ollama`, or `llamacpp` backend instead, or point `openai-compat` at a lenient gateway."
          },
          "talaria.autocomplete.endpoint": {
            "order": 3,
            "type": "string",
            "default": "",
            "scope": "machine",
            "markdownDescription": "Base URL for the autocomplete backend (may be a remote node). Leave empty to use the backend's default (Ollama `http://127.0.0.1:11434`, llama.cpp `http://127.0.0.1:8080`, vLLM `http://127.0.0.1:8000`). Machine-scoped: a workspace cannot redirect where your code context is sent (security)."
          },
          "talaria.autocomplete.model": {
            "order": 4,
            "type": "string",
            "default": "qwen2.5-coder:1.5b-base",
            "scope": "machine",
            "description": "Model name/id used for FIM completions. Machine-scoped: a workspace cannot silently repoint completions at a different model (security)."
          },
          "talaria.autocomplete.apiKey": {
            "order": 5,
            "type": "string",
            "default": "",
            "scope": "machine",
            "markdownDescription": "**Deprecated — use the `Talaria: Set Autocomplete API Key` command instead**, which stores the key in the OS keychain (SecretStorage). API key for backends that require auth (e.g. `codestral`, `openai-compat`). A value here is migrated into SecretStorage on activation and then cleared. Machine-scoped: not workspace-overridable (security)."
          },
          "talaria.autocomplete.debounceMs": {
            "order": 6,
            "type": "number",
            "default": 350,
            "description": "Debounce delay (ms) before triggering a completion request."
          },
          "talaria.autocomplete.maxPromptTokens": {
            "order": 7,
            "type": "number",
            "default": 1024,
            "description": "Token budget for the assembled prefix/suffix prompt."
          },
          "talaria.autocomplete.temperature": {
            "order": 8,
            "type": "number",
            "default": 0.01,
            "description": "Sampling temperature for completions."
          },
          "talaria.autocomplete.crossFile.enabled": {
            "order": 9,
            "type": "boolean",
            "default": true,
            "description": "Gather cross-file context (recently-edited and recently-opened files) for autocomplete, when the configured backend/model support a cross-file channel. Has no effect when the backend/model expose none (falls back to single-file completion)."
          },
          "talaria.autocomplete.crossFile.prefixInjection": {
            "order": 10,
            "type": "boolean",
            "default": false,
            "markdownDescription": "Inject cross-file snippets as comments into the prompt for backends with no native cross-file channel (Ollama, Codestral, openai-compat). **Loopback-gated by default**: only takes effect when `#talaria.autocomplete.endpoint#` resolves to a loopback host (127.0.0.1/localhost), unless `#talaria.autocomplete.crossFile.prefixInjectionRemote#` is also enabled — so turning this on for a local Ollama does not silently start sending workspace snippets to a remote endpoint. A low-signal channel; default-off."
          },
          "talaria.autocomplete.crossFile.prefixInjectionRemote": {
            "order": 11,
            "type": "boolean",
            "default": false,
            "scope": "machine",
            "markdownDescription": "**Security-sensitive.** Allows `#talaria.autocomplete.crossFile.prefixInjection#` to take effect for a REMOTE (non-loopback) `#talaria.autocomplete.endpoint#` too, sending workspace code snippets from OTHER files to that endpoint (not just the active file's prefix/suffix, which is already sent). Leave off unless you trust the configured remote endpoint with your workspace's code. Machine-scoped: a workspace cannot enable this on its own (security)."
          },
          "talaria.autocomplete.crossFile.warmUp": {
            "order": 12,
            "type": "boolean",
            "default": false,
            "markdownDescription": "Experimental: fire a llama.cpp KV-cache warm-up request (`/infill` with an empty prompt, snippets only) whenever the cross-file snippet set regenerates, so the next real completion hits a warm cache. Only takes effect with the `llamacpp` backend (other backends ignore this setting) and still honors the same untrusted-remote/enabled guards as regular completions. Default-off; a latency optimization, not required for cross-file completions to work."
          }
        }
      },
      {
        "title": "Next Edit",
        "order": 3,
        "properties": {
          "talaria.nextEdit.source": {
            "order": 1,
            "type": "string",
            "enum": [
              "off",
              "dedicated",
              "generic"
            ],
            "enumDescriptions": [
              "Next Edit disabled. Default.",
              "A separate, NEXT-specialized model — served via the talaria.nextEdit.backend / .endpoint / .model settings (needs its own setup).",
              "Reuses your existing FIM autocomplete model and endpoint — no extra setup beyond FIM itself."
            ],
            "default": "off",
            "scope": "machine",
            "markdownDescription": "Which engine produces Next Edit (NEXT) multi-line suggestions. `off` (default) disables NEXT. `generic` reuses your FIM autocomplete model on `#talaria.autocomplete.endpoint#` — no extra setup beyond FIM itself. `dedicated` uses a separate NEXT model configured via `#talaria.nextEdit.backend#`, `#talaria.nextEdit.endpoint#` and `#talaria.nextEdit.model#`. Only one source can be active at a time. Ignored while `#talaria.autocomplete.enabled#` is off for `generic`-mode suggestions. Machine-scoped: this setting decides whether — and, via `generic` vs `dedicated`, to which configured backend — editor context is sent for next-edit suggestions; a workspace cannot switch it on by itself (security — same rationale as `#talaria.nextEdit.backend#`)."
          },
          "talaria.nextEdit.backend": {
            "order": 2,
            "type": "string",
            "enum": [
              "ollama",
              "openai-compat"
            ],
            "default": "ollama",
            "scope": "machine",
            "description": "Transport for Next Edit Suggestions (the NEXT source). Generic always uses the FIM model on `#talaria.autocomplete.endpoint#`, never this setting. Machine-scoped: a workspace cannot redirect where next-edit context is sent (security)."
          },
          "talaria.nextEdit.endpoint": {
            "order": 3,
            "type": "string",
            "default": "",
            "scope": "machine",
            "markdownDescription": "Base URL for the Next Edit Suggestions (NEXT) backend. Leave empty to use the backend's default. Generic always uses `#talaria.autocomplete.endpoint#` instead of this setting. Machine-scoped: a workspace cannot redirect where next-edit context is sent (security)."
          },
          "talaria.nextEdit.model": {
            "order": 4,
            "type": "string",
            "default": "",
            "scope": "machine",
            "description": "Model name/id used for Next Edit Suggestions (the NEXT source), e.g. a sweep-v2-7B GGUF import. Machine-scoped: a workspace cannot silently repoint next-edit requests at a different model (security)."
          },
          "talaria.nextEdit.dedicatedBackendId": {
            "order": 5,
            "type": "string",
            "default": "",
            "scope": "machine",
            "markdownDescription": "Which unified Local Model pane (`ollama` / `llamacpp` / `vllm` / `openai-compat`) the dedicated Next Edit connection was last configured through — restoration-only, read by the Setup panel to reopen the same pane. Leave empty to fall back to the existing transport heuristic. Set through the panel, not hand-edited. Machine-scoped: not workspace-overridable (security — consistent with every other `#talaria.nextEdit#` key)."
          }
        }
      },
      {
        "title": "RAG (Codebase Index)",
        "order": 4,
        "properties": {
          "talaria.rag.enabled": {
            "order": 1,
            "type": "boolean",
            "default": true,
            "description": "Enable the codebase RAG indexer and MCP search server."
          },
          "talaria.rag.embedEndpoint": {
            "order": 2,
            "type": "string",
            "default": "http://127.0.0.1:11434",
            "scope": "machine",
            "description": "Base URL for the embeddings backend (Ollama/llama.cpp/vLLM-compatible; may be a remote node). Machine-scoped: a workspace cannot redirect where whole-file embeddings are sent (security)."
          },
          "talaria.rag.embedModel": {
            "order": 3,
            "type": "string",
            "default": "qwen3-embedding:0.6b",
            "scope": "machine",
            "description": "Model name/id used to embed code chunks. Machine-scoped: a workspace cannot silently repoint embeddings at a different model (integrity — same rationale as talaria.autocomplete.model)."
          },
          "talaria.rag.dims": {
            "order": 4,
            "type": "number",
            "default": 0,
            "markdownDescription": "Matryoshka truncation width for embeddings, forwarded as the OpenAI `dimensions` field. **`0` (default) means: do not send the field at all and let the server decide.** Only set a positive value if your model and runner support it — llama.cpp ignores the field, Ollama truncates, and vLLM answers HTTP 400 for a non-Matryoshka model, which fails the whole index build. Changing this value changes the index fingerprint and forces a full rebuild."
          },
          "talaria.rag.maxChunkTokens": {
            "order": 5,
            "type": "number",
            "default": 512,
            "description": "Approximate max tokens per indexed chunk."
          },
          "talaria.rag.indexDir": {
            "order": 6,
            "type": "string",
            "default": ".hermes/index",
            "scope": "machine",
            "description": "Workspace-relative directory where the vector index is stored. Machine-scoped: not workspace-overridable (security)."
          },
          "talaria.rag.debounceMs": {
            "order": 7,
            "type": "number",
            "default": 500,
            "description": "File-watcher debounce (ms) before incremental reindexing."
          },
          "talaria.rag.excludeGlobs": {
            "order": 8,
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [],
            "description": "Extra glob patterns to exclude from indexing, beyond .gitignore/.hermesignore."
          },
          "talaria.rag.embedBackend": {
            "order": 9,
            "type": "string",
            "enum": [
              "ollama",
              "llamacpp",
              "openai-compat"
            ],
            "default": "ollama",
            "scope": "machine",
            "markdownDescription": "Which backend the RAG embedder block (`#talaria.rag.embedEndpoint#` / `#talaria.rag.embedModel#`) is configured against — restoration-only, read by the Setup panel to reopen the same pane. Set through the panel, not hand-edited. Machine-scoped: a workspace cannot redirect where whole-file embeddings are sent (security — same rationale as `#talaria.rag.embedEndpoint#`)."
          }
        }
      },
      {
        "title": "Advanced",
        "order": 5,
        "properties": {
          "talaria.dashboardPort": {
            "order": 1,
            "type": "number",
            "default": 9119,
            "scope": "machine",
            "markdownDescription": "Loopback port for the Hermes dashboard REST server (`http://127.0.0.1:<port>`) that powers the real Skills & Tools panels. If a dashboard is already listening there it is adopted; otherwise the extension spawns `python -m hermes_cli.main serve --port <port>` (Linux). Ignored while `#talaria.backend#` is `mock`. Machine-scoped: a workspace cannot repoint it (security)."
          },
          "talaria.dashboardAdopt": {
            "order": 2,
            "type": "string",
            "enum": [
              "spawn-only",
              "shape"
            ],
            "enumDescriptions": [
              "Secure default. Never adopts a pre-existing dashboard: always spawns and provenance-verifies its own child before use.",
              "Legacy, INSECURE opt-in: adopts any already-running responder whose /api/status body merely LOOKS like Hermes, with no further verification. Only for coexisting with an already-running Hermes desktop holding the port."
            ],
            "default": "spawn-only",
            "scope": "machine",
            "markdownDescription": "How the extension discovers the Hermes dashboard REST server on `#talaria.dashboardPort#`. `spawn-only` (default, secure) never adopts a foreign peer — it always spawns its own child and verifies the healthy server's served session token has the provenance of that spawn before use (CWE-306/346). `shape` is a legacy, INSECURE opt-in that adopts any responder whose `/api/status` body is merely Hermes-shaped, which a local process could spoof; use it only to coexist with an already-running Hermes desktop instance. Ignored while `#talaria.backend#` is `mock`. Machine-scoped: a workspace cannot weaken this (security)."
          },
          "talaria.lib.enabled": {
            "order": 3,
            "type": "boolean",
            "default": true,
            "scope": "machine",
            "markdownDescription": "Expose the editor's live language-server intelligence (diagnostics, definitions, references, symbols, hover) to the Hermes agent as read-only MCP tools over an authenticated loopback server. Read-only; requires a trusted workspace (ignored in Restricted Mode). Machine-scoped: a workspace cannot enable it (security)."
          },
          "talaria.customModes": {
            "order": 4,
            "type": "array",
            "items": {
              "type": "object",
              "required": [
                "id",
                "name"
              ],
              "properties": {
                "id": {
                  "type": "string",
                  "description": "Stable identifier for this mode (used on the wire and to match it up when re-selected)."
                },
                "name": {
                  "type": "string",
                  "description": "Display name shown in the mode picker."
                },
                "deny": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "Rules the agent may NOT edit: an exact workspace-relative path, a directory prefix ending '/', or a basename suffix '*.ext'."
                },
                "allowOnly": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "description": "If set, ONLY paths matching one of these rules may be edited; everything else is denied (same rule grammar as deny)."
                }
              }
            },
            "default": [],
            "scope": "resource",
            "markdownDescription": "Custom modes restrict which files the agent may edit on the main edit path (`write_file`/`patch`). Rules: exact workspace-relative path (`src/app.ts`), directory prefix ending `/` (`src/`), or basename suffix (`*.env`). No globs/`**`/negation. A mode is a strong floor on the main edit path, **not a sandbox** — terminal, code execution, subagent, and MCP-tool writes bypass it; recovery for those is the post-turn checkpoint."
          }
        }
      }
    ]
  },
  "scripts": {
    "build": "npm run build:host && npm run build:webview",
    "build:host": "node esbuild.js",
    "build:webview": "cd webview && vite build",
    "watch:host": "node esbuild.js --watch",
    "watch:webview": "cd webview && vite build --watch",
    "watch": "concurrently -n host,webview -c blue,green \"npm:watch:host\" \"npm:watch:webview\"",
    "check-types": "tsc --noEmit",
    "check-types:all": "npm run check-types && npm run typecheck -w webview",
    "package": "vsce package",
    "package:linux-x64": "vsce package --target linux-x64",
    "package:linux-arm64": "vsce package --target linux-arm64",
    "vscode:prepublish": "npm run build:host -- --production && npm run build:webview",
    "pretest": "npm run build",
    "test": "vitest run",
    "pretest:integration": "node -e \"require('fs').rmSync('.vscode-test/user-data',{recursive:true,force:true})\" && npm run build && tsc -p test/integration/tsconfig.json",
    "test:integration": "vscode-test",
    "gate": "npm run check-types:all && npm test"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "0.17.1",
    "@lancedb/lancedb": "^0.33.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "apache-arrow": "18.1.0",
    "ignore": "^7.0.6",
    "tree-sitter-wasms": "0.1.13",
    "web-tree-sitter": "0.25.10",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.10.0",
    "@types/vscode": "^1.125.0",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.1.0",
    "@vscode/vsce": "^3.9.2",
    "concurrently": "^10.0.4",
    "esbuild": "^0.28.1",
    "jsdom": "^29.1.1",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  },
  "overrides": {
    "vitest": {
      "expect-type": "1.4.0"
    }
  }
}
